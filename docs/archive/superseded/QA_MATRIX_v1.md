> Status: Non-canonical
> Type: Archive
> Authority: Not the primary source of truth when a canonical doc exists

# QA Matrix V1 (Historical)

Status: Archived V1 QA baseline.

This matrix references legacy V1 expectations and should not be used as the release gate for current development.

Current validation commands:
- `cd apps/api && npm run typecheck && npm run lint && npm test`
- `cd apps/client && npm run typecheck && npm run lint && npm test`
